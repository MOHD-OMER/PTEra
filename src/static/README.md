# Static Assets

This directory contains static assets for the PTE Mock Test application:

- `style.css`: Custom styles for the Streamlit interface
- `images/`: Application images and icons
- `audio/`: Directory for temporary audio files during listening tests

Note: Audio files are generated and cleaned up automatically during test sessions. 